"""Topon Streamlit GUI — currently a scaffold.

Run with:

    streamlit run topon/gui/app.py

or from the CLI:

    topon gui

The CLI sub-command checks whether ``streamlit`` is importable and prints a
friendly message if not. Install via ``pip install topon[gui]`` (the
``gui`` extra in pyproject.toml pulls in streamlit).
"""
